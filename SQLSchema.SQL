-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public."Log"
(
    id serial NOT NULL,
    id_user integer NOT NULL,
    id_segment integer NOT NULL,
    operation character varying(50) COLLATE pg_catalog."default" NOT NULL,
    "timestamp" timestamp without time zone NOT NULL,
    CONSTRAINT "Log_pkey" PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public."Segment"
(
    id serial NOT NULL,
    name character varying(100) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT "Segment_pkey" PRIMARY KEY (id),
    CONSTRAINT "Segment_name_key" UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS public."User"
(
    id serial NOT NULL,
    CONSTRAINT "User_pkey" PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public."UserSegment"
(
    id_user integer NOT NULL,
    id_segment integer NOT NULL,
    CONSTRAINT "UserSegment_pkey" PRIMARY KEY (id_segment, id_user)
);

ALTER TABLE IF EXISTS public."Log"
    ADD FOREIGN KEY (id_segment)
    REFERENCES public."Segment" (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public."Log"
    ADD FOREIGN KEY (id_user)
    REFERENCES public."User" (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public."UserSegment"
    ADD CONSTRAINT "UserSegment_id_segment_fkey" FOREIGN KEY (id_segment)
    REFERENCES public."Segment" (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;


ALTER TABLE IF EXISTS public."UserSegment"
    ADD CONSTRAINT "UserSegment_id_user_fkey" FOREIGN KEY (id_user)
    REFERENCES public."User" (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION
    NOT VALID;

END;